pub fn help_msg(){
    let message = r#"
            use de -h or --help commands for getting complete information
            try to append the exact command you want information from

            lee -h new (for example)
        "#;
    println!("{}", message);
} 

